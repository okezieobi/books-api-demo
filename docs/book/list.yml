paths:
  /books: # path of the user from your endpoint
    get: # endpoint request type (post request)
      tags: # Tag property
        - Book # Value of the tag
      summary: 'fetch books to list'
      parameters:
        - name : page
          in: query
          description: 'Page number of books to list'
          schema:
            type: number
        - name : size
          in: querys
          description: 'Page size of books to list'
          schema:
            type: number
        - name : gender
          in: query
          description: 'gender attribute to filter list of books'
          schema:
            type: number
      responses: # server responses
        200:
          description: 'An object containing profile details'
